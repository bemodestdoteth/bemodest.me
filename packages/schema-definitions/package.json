{
    "name": "@bemodest/schema-definitions",
    "version": "1.0.0",
    "description": "JSON Schemas for polyglot type generation",
    "scripts": {
        "generate:json": "node --import tsx src/generate.ts",
        "generate:ts": "quicktype -s schema schemas/AlertRule.json schemas/SidecarConfigPayload.json schemas/NormalizedTicker.json schemas/SystemConfig.json -o ../types/src/generated.ts --just-types",
        "build": "pnpm run generate:json && pnpm run generate:ts"
    },
    "devDependencies": {
        "quicktype": "^23.0.170",
        "tsx": "^4.21.0",
        "zod-to-json-schema": "^3.25.1"
    }
}