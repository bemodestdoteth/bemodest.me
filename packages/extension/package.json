{
  "name": "@bemodest/extension",
  "version": "0.1.0",
  "private": true,
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "vite --config vite.config.js build",
    "watch": "vite --config vite.config.js build --watch",
    "clean": "rm -rf dist node_modules/.vite .vite"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "vite": "^7.1.3"
  }
}
